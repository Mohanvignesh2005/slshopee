{
  "name": "sl-shopee-deployment",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix backend install",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": "20.x"
  }
}
